# AI templates

Run the `Initialize DeepSeek Template` workflow once after configuring
`DEEPSEEK_API_KEY`.

The workflow creates:

- `templates/home.html`
- `templates/article.html`
- `templates/template.lock.json`
- `assets/css/style.css`

The lock file prevents later template regeneration.
